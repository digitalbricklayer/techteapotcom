[build]
  # Directory to change to before starting a build.
  # This is where we will look for package.json/.nvmrc/etc.
  base = ""

  # Directory that contains the deploy-ready HTML files and assets generated by
  # the build. This is relative to the base directory if one has been set, or the
  # root directory if a base has not been set. This sample publishes the
  # directory located at the absolute path "root/project/build-output"
  publish = "public/"

  # Default build command - picks up HUGO_ENV defined inside context
  command = "hugo"

# Production context: this is the live website
[context.production]

[context.production.environment]
  HUGO_ENV = production

# Deploy Preview context: all deploys generated from a pull/merge request will
# inherit these settings.
#[context.deploy-preview]
#  publish = "dist/"

# Branch Deploy context: all deploys that are not from a pull/merge request or
# from the Production branch will inherit these settings.
#[context.branch-deploy]
#  command = "echo branch"
#[context.branch-deploy.environment]
#  NODE_ENV = "development"

# 'develop' is where site development happens
[context.develop]

[context.develop.environment]
  HUGO_ENV = development

# For contexts of branches with special characters, enclose the branch name
# with quotes.
#[context."feat/branch"]
#  command = "echo 'special branch'"
#  base = "branch"
